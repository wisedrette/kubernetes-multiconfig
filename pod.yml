 apiVersion: v1
 kind: Pod
 metadata:
   name: rss-site
   labels:
     app: web
 spec:
   containers:
     - name: test
       image: registrytest-docker.jfrog.io/custom-image:latest
       imagePullPolicy: Always
       ports:
         - containerPort: 80
   imagePullSecrets:
     - name: squashregistrykey
