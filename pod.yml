 apiVersion: v1
 kind: Pod
 metadata:
   name: rss-site
   labels:
     app: web
 spec:
   containers:
     - name: test
       image: r.cfcr.io/wisedrette/wisedrette/dockertest:master
       imagePullPolicy: Always
       ports:
         - containerPort: 80
   imagePullSecrets:
     - name: squashregistrykey
